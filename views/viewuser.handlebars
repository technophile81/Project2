THIS IS A USER NAMED {{profile.name}}

        <div class="following">
            <button id="followed-button-{{profile.userId}}" class="followed" data-userid="{{profile.userId}}">
                {{#if profile.Followers}}
<i class="foo fas fa-user"></i> 
                {{else}}
<i class="foo far fa-user"></i>
                {{/if}}
            </button>
        </div>

        <script src="../js/profilepage.js" type="text/javascript"></script>